# 기획 문서 — DJ News Desk

작성: 2026-08-28

## 왜 만드나

관심 분야 5개(M&A / K-Food / 정치 / 경제 / 세계)의 뉴스를 매일 흩어진 곳에서 찾아 읽는 대신,
한 페이지에서 하루 2번 확인한다. 모바일에서 열리는 게 전제다.

기존 `01.DJ_vibecode_HQ/Digest_bot`(텔레그램 푸시)과 역할이 다르다.
Digest_bot은 밀어주는 알림, News Desk는 내가 찾아가 훑는 대시보드.

## 확정한 결정

| 항목 | 결정 | 이유 |
|---|---|---|
| 비용 | **0원** | AI 요약 API 미사용. 기사 스니펫으로 대체 |
| 수집 소스 | 구글뉴스 RSS(현재) + 네이버 검색 API(키 등록 시) | 둘 다 무료. 언론사 RSS는 자리만 열어둠 |
| 호스팅 | GitHub Pages + GitHub Actions | 서버 관리 0, 무료 |
| 갱신 | 하루 2회 07:00 / 19:00 KST | 출근길·퇴근길 |
| 화면 | 홈=대시보드 + 카테고리 탭 | 전체 흐름과 깊이 파기 둘 다 |
| 부가기능 | 북마크 · 키워드검색 · 아카이브 · 중복묶기 | 전부 채택 |

## 카테고리 범위

- **M&A** — 국내 경영권 거래 / 스타트업 투자·엑시트 / 크로스보더 / 구조조정·회생 (4개 전부)
- **K-Food** — F&B 프랜차이즈·외식 트렌드 중심
- **정치** — 한국 국회·대통령실·정책
- **경제** — 한국 증시·금리·환율·부동산
- **세계** — 네이버 뉴스 '세계' 탭 수준의 브로드한 국제 뉴스

## 기술적으로 풀어야 했던 것

**1. 중복 기사 묶기**
같은 사건을 30개 매체가 쓴다. 제목을 정규화(대괄호 태그·언론사 꼬리표·공백 제거)한 뒤
문자 2-gram 자카드 유사도로 묶었다.

측정해보니 같은 사건 쌍은 0.29~0.46, 다른 사건 쌍은 0.00으로 간격이 컸다.
임계값 **0.32**로 잡고, 클러스터 대표 하나가 아니라 **이미 묶인 기사 전부와 비교**하도록 했다.
같은 사건도 제목 표현이 갈려서 연쇄로 이어붙어야 제대로 묶인다.
→ 삼성바이오 유상증자 기사 30건이 카드 1장(+29건)으로 정리됐다.

**2. 출처가 포털로 잡히는 문제**
구글뉴스가 `v.daum.net`, `네이트` 같은 재게시본을 대표로 준다.
포털 목록을 두고, 묶인 기사 중 실제 언론사가 있으면 그쪽을 대표로 올린다.

**3. 블로그·카페 유입**
`blog.naver.com`, 티스토리, 브런치, 유튜브 등을 호스트·소스명 양쪽으로 차단.
K-Food 카테고리에서 90건 중 14건이 걸렸다.

**4. 요약 스니펫**
구글뉴스 RSS는 `<description>`에 관련기사 링크 목록만 준다. 요약이 아니다.
**네이버 검색 API는 진짜 스니펫을 준다** → 키를 넣으면 카드에 2줄 미리보기가 붙는다.
현재는 제목 중심 + 중복 묶음 개수로 정보량을 확보하고 있다.

## 1세대 버전에서 가져온 것 (`기존버전/` 참고)

이 프로젝트는 두 번째 시도다. 1세대는 claude.ai 예약 루틴이 매일 `index.html`을
직접 다시 써서 GitHub Pages에 올리는 방식이었다. 핸드오프 문서에서 얻은 교훈:

**1세대가 겪은 문제 — 이번 구조에서 어떻게 해결됐나**

| 1세대 문제 | 이번 구조 |
|---|---|
| LLM이 매일 `index.html`을 통째로 다시 써서, 수동 추가한 K-푸드 탭이 매일 사라졌다 | 데이터(JSON)와 화면(HTML)을 분리했다. 카테고리는 `config.json`에서 오고 화면은 건드리지 않는다 |
| claude.ai GitHub 커넥터 403 오류로 자동 푸시가 몇 주간 실패 | GitHub Actions가 같은 저장소에 커밋한다. 외부 커넥터 권한이 필요 없다 |
| LLM이 기사를 매일 새로 써서 수치를 지어낼 위험 (프롬프트로 "숫자 지어내지 말 것" 방어) | 수집기는 RSS 원문을 그대로 옮기기만 한다. 생성하는 텍스트가 없어 지어낼 여지 자체가 없다 |
| 매일 LLM 호출 → 토큰 비용 발생 | LLM 호출 0회, 비용 0원 |
| UTC/KST 날짜가 어긋나 배너 날짜가 하루 밀려 보임 | 전 구간 KST로 계산한다 |

**1세대에서 가져온 기능**

- **시세 카드** — 두영이 명시적으로 요청했던 국장(코스피·코스닥) / 미장(다우·S&P·나스닥) /
  환율(원·달러) / 반도체주(삼성전자·SK하이닉스) 구성을 경제 탭에 그대로 살렸다.
  네이버 금융 모바일 엔드포인트라 키가 필요 없다.

**1세대에서 일부러 안 가져온 것**

- **상단 "오늘의 요약 브리핑"** — 1세대에서 넣었다가 "보기 불편하다"는 이유로 제거됐다. 다시 넣지 않는다.
- **좌측 폴더 네비게이션("1. 뉴스 클리핑" / "2. 무제")** — 이번엔 상단 탭 구조를 선택했다.

**딜 테이블 — DART로 해결했다**

1세대에서는 LLM이 웹서치 결과를 읽고 표를 채웠다. 이번에는 소스를 바꿔서 무료로 해결했다.

- `thevc.kr` 은 403으로 완전히 막혀 있어 접근 불가
- 기사 제목에서 정규식으로 뽑아보니 채움률이 형편없었다
  (16행 기준 기업가치 1건, 지분율 0건, 투자자 1건). Target도 서술구가 딸려와 오추출이 났다
- **DART 전자공시**로 바꾸니 상장사 딜은 전부 채워진다.
  15행 기준 Target 15 / 분야 15 / 투자금액 15 / 지분율 11.
  상장사는 공시 의무가 있어 금액이 원 단위까지 정확하다

지금은 **DART(상장사 딜) + 뉴스 추출(비상장 VC 라운드)** 를 합쳐 한 표로 보여준다.
비어 있는 칸은 추정하지 않고 `정보 없음`으로 남긴다 — 1세대에서 확인된 원칙 그대로다.
기업가치(EV)는 공시의 외부평가 의견이 자유 서술이라 숫자로 옮기지 않는다.

## 지켜야 할 원칙 (1세대에서 확인된 것)

- 수치를 지어내지 않는다. 출처에 없으면 비워둔다.
- 기사 링크는 실제 원문 URL이어야 한다. 홈페이지나 목록 페이지 링크는 안 된다.
- 상단 요약 브리핑은 넣지 않는다.

## 앞으로 열어둔 것

- 언론사 RSS 직접 지정 (`config.json` → `publisher_rss.enabled = true`)
- 카테고리 추가 — config에 항목만 넣으면 프론트가 탭을 자동 생성
- 관심 키워드 알림(텔레그램 연동) — Digest_bot과 합칠 여지
